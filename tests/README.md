# Tests

These are static tests. They validate the installer contract without running OpenVPN or changing the host firewall.

Run:

```bash
./run-tests.sh
```

They are expected to fail on unmodified upstream and pass after implementation.
